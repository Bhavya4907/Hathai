package com.chatapp;

import javafx.application.Application;

public class Launcher {
    public static void main(String[] args) {
        // Replace 'ChatLogin' with whatever screen you want to open first
        Application.launch(ChatSignup.class, args);
    }
}